name: build

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  go:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true
      - name: check go.mod/go.sum are tidy
        run: go mod tidy -diff
      - run: go build ./...
      - run: go vet ./...
      - run: go test ./...

  image:
    needs: go
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: github.event_name != 'pull_request'
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5
      # No QEMU: the Dockerfile cross-compiles from the build host and the
      # runtime stage has no RUN steps, so nothing executes as arm64.
      - uses: docker/setup-buildx-action@v3
      - name: docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: rayos006/lightsail-cert-manager-webhook
          # Keep the v prefix (v0.1.0) — the chart's values.yaml image.tag
          # references the v-prefixed form.
          tags: |
            type=semver,pattern=v{{version}}
            type=semver,pattern=v{{major}}.{{minor}}
            type=ref,event=branch
            type=sha,format=short
      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  chart:
    needs: go
    runs-on: ubuntu-latest
    timeout-minutes: 5
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v5
      - uses: azure/setup-helm@v4
        with:
          version: latest
      - name: derive version from tag
        id: v
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
      - name: bump chart version to match tag
        run: |
          sed -i "s/^version:.*/version: ${{ steps.v.outputs.version }}/" deploy/lightsail-webhook/Chart.yaml
          sed -i "s/^appVersion:.*/appVersion: \"${{ steps.v.outputs.version }}\"/" deploy/lightsail-webhook/Chart.yaml
      - name: helm lint
        run: helm lint deploy/lightsail-webhook
      - name: helm package
        run: helm package deploy/lightsail-webhook -d /tmp
      - name: helm login (ghcr)
        run: echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ghcr.io -u ${{ github.actor }} --password-stdin
      - name: helm push
        run: |
          helm push /tmp/lightsail-webhook-${{ steps.v.outputs.version }}.tgz \
            oci://ghcr.io/${{ github.repository_owner }}/charts
